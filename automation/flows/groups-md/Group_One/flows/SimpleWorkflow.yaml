name: SimpleWorkflow
phase: draft
description: 'Overview: Record-based pipeline that normalizes a field and branches
  on a SQL predicate.

  Technical: GenerateRecord (embedded Avro schema) emits fields ''status'' and ''value''.
  UpdateRecord uppercases'
processors: []
connections: []
